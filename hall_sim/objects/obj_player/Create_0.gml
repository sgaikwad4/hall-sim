// Create Event
normal_speed = 4;        // Normal movement speed
slowdown_factor = 0.25;   // How much the speed should be reduced (50%)
slowdown_duration = 5;   // Duration of the slowdown in seconds
slowdown_timer = 0;      // Timer to track the slowdown duration
is_slowed = false;       // Flag to check if the object is slowed down
